# List of resources.
# Each resource has contains a list of 2 items.
# The first item is the name of the resource.
# The second item are the properties of the resource specified as a dictionary.
- network:
  - main
  - cidr_block: 10.100.0.0/16
- subnet:
  - public_subnet
  - cidr_block: 10.100.1.0/24
- subnet:
  - private_subnet
  - cidr_block: 10.100.2.0/24
    tags:
        Name: private-subnet